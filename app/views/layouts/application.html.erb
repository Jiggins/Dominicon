<!DOCTYPE html>
<html>
  <head>
    <title>Dominicon</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">

          <%# Menu button for mobile devices %>
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-main-collapse" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <%# The three bars in the menu button %>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>

          <!-- Tag the navbar with a environment label -->
          <%= link_to "/", class: "navbar-brand" do %>
            <% if Rails.env.production? %>
              <%= 'Dominicon' %>
            <% else %>
              <%= 'Dominicon' %> <span class="label label-success"><%= Rails.env %></span> 
            <% end %>
          <% end %>
        </div>

        <%# Navbar left align %>
        <div class="collapse navbar-collapse" id="navbar-main-collapse">

          <%# See: app/helpers/application_helper.rb %>
          <%= nav_bar do %>
            <%= nav_link "About", about_path %>
          <% end %>

        </div>
      </div>
    </nav>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
